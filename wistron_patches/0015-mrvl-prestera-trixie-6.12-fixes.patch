mvcpss/mvSai driver fixes for trixie/kernel 6.12 (Marvell upstream not pushable).

diff --git a/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/dmaDriver2.c b/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/dmaDriver2.c
index 278fec0..1c8f6cd 100644
--- a/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/dmaDriver2.c
+++ b/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/dmaDriver2.c
@@ -777,13 +777,19 @@ static int mvdma_pdriver_probe(struct platform_device *pdev)
 	return 0;
 };
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,11,0)
+static void mvdma_pdriver_remove(struct platform_device *pdev)
+#else
 static int mvdma_pdriver_remove(struct platform_device *pdev)
+#endif
 {
 	BUG_ON(!platform_dev_mappings);
 #if (LINUX_VERSION_CODE > KERNEL_VERSION(3,19,8))
 	of_reserved_mem_device_release(&pdev->dev);
 #endif
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6,11,0)
 	return 0;
+#endif
 }
 
 static const struct of_device_id mvdma_of_match_ids[] = {
diff --git a/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/ethDriver.c b/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/ethDriver.c
old mode 100755
new mode 100644
index 012eddc..95413e6
--- a/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/ethDriver.c
+++ b/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/ethDriver.c
@@ -4229,7 +4229,11 @@ out:
 	return 0;
 };
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,11,0)
+static void mvppnd_pdriver_remove(struct platform_device *pdev)
+#else
 static int mvppnd_pdriver_remove(struct platform_device *pdev)
+#endif
 {
 	struct mvppnd_dev *ppdev;
 
@@ -4237,7 +4241,11 @@ static int mvppnd_pdriver_remove(struct platform_device *pdev)
 
 	ppdev = (struct mvppnd_dev *)dev_get_drvdata(&pdev->dev);
 	if (!ppdev)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,11,0)
+		return;
+#else
 		return 0;
+#endif
 
 	ppdev->going_down = true;
 
@@ -4252,7 +4260,9 @@ static int mvppnd_pdriver_remove(struct platform_device *pdev)
 
 	dev_info(&pdev->dev, "Detached from device\n");
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6,11,0)
 	return 0;
+#endif
 }
 
 static const struct of_device_id mvppnd_of_match_ids[] = {
diff --git a/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/mvDriverTemplate.h b/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/mvDriverTemplate.h
index 13424e0..13f9c8f 100644
--- a/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/mvDriverTemplate.h
+++ b/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/mvDriverTemplate.h
@@ -52,7 +52,9 @@ struct mvchrdev_ctx {
 };
 
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(2,6,32)
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(3,4,69)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,2,0)
+static char *mvchrdev_devnode(const struct device *dev, umode_t *mode)
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(3,4,69)
 static char *mvchrdev_devnode(struct device *dev, umode_t *mode)
 #else /* < 3.4.69 */
 static char *mvchrdev_devnode(struct device *dev, mode_t *mode)
@@ -107,7 +109,11 @@ static struct mvchrdev_ctx *mvchrdev_init(const char *name,
 		goto err_unreg_drv;
 	}
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,4,0)
+	ctx->class = class_create(name);
+#else
 	ctx->class = class_create(THIS_MODULE, name);
+#endif
 	if (IS_ERR(ctx->class)) {
 		pr_err("%s: Fail to create class (%d)\n", name, rc);
 		goto err_del_cdev;
diff --git a/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/saiMod.c b/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/saiMod.c
index cf662f9..84d7202 100644
--- a/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/saiMod.c
+++ b/platform/marvell-prestera/mrvl-prestera/drivers/generic/cpssEnabler/linuxNoKernelModule/drivers/saiMod.c
@@ -657,7 +657,11 @@ static int __init sai_init(void)
 
     major = err;
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,4,0)
+    sai_class = class_create(SAI_NAME);
+#else
     sai_class = class_create(THIS_MODULE, SAI_NAME);
+#endif
     if (IS_ERR(sai_class)) {
         err = PTR_ERR(sai_class);
         goto out_chrdev;
diff --git a/platform/marvell-prestera/mrvl-prestera/platform/arm64/913x/etc/modules-load.d/marvell.conf b/platform/marvell-prestera/mrvl-prestera/platform/arm64/913x/etc/modules-load.d/marvell.conf
index eec44bb..42adf2a 100644
--- a/platform/marvell-prestera/mrvl-prestera/platform/arm64/913x/etc/modules-load.d/marvell.conf
+++ b/platform/marvell-prestera/mrvl-prestera/platform/arm64/913x/etc/modules-load.d/marvell.conf
@@ -1,3 +1,4 @@
 eeprom
 mvcpss
 psample
+mvSai
